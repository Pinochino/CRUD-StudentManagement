CREATE TABLE Students (
                          id SERIAL PRIMARY KEY,
                          email VARCHAR(45),
                          first_name VARCHAR(45),
                          last_name VARCHAR(45)
);

