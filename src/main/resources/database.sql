CREATE TABLE Students (
                          id SERIAL PRIMARY KEY,
                          fullname VARCHAR(45),
                            email VARCHAR(45),
                          password VARCHAR(45),
                          sex VARCHAR(10)
);

